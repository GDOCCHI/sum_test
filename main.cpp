// main.cpp
#include <stdio.h>
#include "sum.h"

int main() {
	char tarck[] = "취약점";
	char name[] = "조해성";
	printf("[bob14][%s]sum-test[%s]", track, name);
	int s = sum(10);
	printf("result=%d\n", s);
}
